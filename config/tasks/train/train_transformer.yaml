epochs: 40

val_interval: 20
num_val_batches: 40

checkpointing: False

data_loaders:
  train:
    split_key: train_idxs
    batch_size: 200
    num_workers: 24
    # pre_transforms:
    #   - random_reflections
  validation:
    split_key: val_idxs
    batch_size: 200
    num_workers: 24
    drop_last: True